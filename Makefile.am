SUBDIRS = src
EXTRA_DIST = autogen.sh

clean-local:
	@rm .dirstamp

autoclean:
	@rm -rf config.status config.log configure Makefile Makefile.in \
		src/Makefile src/Makefile.in autom4te.cache/ aclocal.m4 build-aux/ \
		autoscan.log configure.scan
